{{ form_start(form, {attr: {class: 'filter'}}) }}
    {{ form_row(form.q) }}

    <h4>Catégories</h4>
    {{ form_row(form.categories) }}

    <h4>Prix</h4>
    <div class="row">
        <div class="col-md">{{ form_row(form.min) }}</div>
        <div class="col-md">{{ form_row(form.max) }}</div>
    </div>

    <div id="price-slider" data-min="{{ min }}" data-max="{{ max }}" class="mb-2"></div>

    <h4>Promotions</h4>
    {{ form_row(form.promo) }}

    <button type="submit" class="btn btn-primary w-100"> <i class="fa fa-search-plus" aria-hidden="true"></i> Filtrer</button>
{{ form_end(form) }}